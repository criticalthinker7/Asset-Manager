# Hostinger / Apache — serve CanGrants SPA from this folder
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /cangrants/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /cangrants/index.html [L]
</IfModule>
